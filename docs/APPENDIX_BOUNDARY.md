# Appendix Boundary

The finite native `B_n` fingerprint appendix records exact `n=8` evidence only.

Current atlas accounting:

```text
41 fixed-plus family-instances
114 defect rows
6 accounted family-instances
44 accounted rows
35 open family-instances
70 open rows
```

The appendix includes two finite zero mechanisms:

1. `(1.1|nu)`, `nu |- 6`, vanishes by pre-Specht group-algebra cancellation in `Q[S_6]`.
2. `(2.2.2.1.1|empty)` vanishes by finite Johnson degree-drop after sign twist.

This does not prove a full native `B_n` fingerprint theorem or classification.
